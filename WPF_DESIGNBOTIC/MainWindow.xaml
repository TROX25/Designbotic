<Window x:Class="WPF_DESIGNBOTIC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_DESIGNBOTIC"
        mc:Ignorable="d"
        Title="Json connector" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>

            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <!-- <Button Content="Wczytaj JSON" Click="LoadJson_Click" Width="100" VerticalAlignment="Center" 
        Grid.Row="0" Grid.Column="1"/> -->
        
        <TreeView x:Name="JsonTreeView" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>